---
name: html-skill
description: Use this skill when generating HTML files
---

## Overview
Generate self-contained HTML files that contains the html tags, javascript code and the css files. No other external dependencies or libraries should be required to execute the HTML files

Ensure the HTML file follows proper syntax
